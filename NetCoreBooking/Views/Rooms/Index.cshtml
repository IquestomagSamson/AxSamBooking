@model IEnumerable<Booking_Room.Models.Room>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<br />
<br />
<button type="button" class="btn btn-success">
    <a asp-action="Create"><span style="color:white">Create New</span></a>
</button>
<br />
<br />
<form asp-controller="Rooms" asp-action="Index" method="get">
    <p>
        Title: <input type="text" name="seacrhString">
        <input type="submit" value="Filter" />
    </p>
</form>

@*<div class="container-xl">
    <br /><br />
    <button type="button" class="btn btn-success">
        <a asp-action="Create"><span style="color:white">Create New</span></a>
    </button>
    <div class="table-responsive">
        <div class="table-wrapper">
            <div class="table-title">
                <div class="row">
                    <div class="col-sm-8"><h2>Booking list</h2></div>
                    <div class="col-sm-4">
                        <form asp-controller="Rooms" asp-action="Index" method="get">
                            <p>
                                Title: <input type="text" name="seacrhString">
                                <input type="submit" value="Filter" />
                            </p>
                        </form>

                    </div>
                </div>
            </div>*@


<table class="table table-striped table-hover table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.room_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.capacity)
        </th>
        <th></th>
    </tr>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.room_name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.capacity)
                </td>

                <td>

                    <ul class="list-inline m-0">

                        @*<a asp-action="Details" asp-route-id="@item.room_id">Details</a> |*@

                        <li class="list-inline-item">
                            @*  @Html.ActionLink("Edit", "Edit", new { id = item.Booking_id.Trim() }, new { @class = "btn btn-success btn-sm rounded-0 " })*@
                            <a asp-action="Edit" asp-route-id="@item.room_id">Edit</a> |
                        </li>

                        <li class="list-inline-item">
                            @*  @Html.ActionLink("Edit", "Edit", new { id = item.Booking_id.Trim() }, new { @class = "btn btn-success btn-sm rounded-0 " })*@
                            <a asp-action="Delete" asp-route-id="@item.room_id">Delete</a>
                        </li>
                    </ul>
                </td>
            </tr>
        }

    </tbody>
</table>


@*<div class="clearfix">
        <div class="hint-text">Showing <b>5</b> out of <b>25</b> entries</div>
        <ul class="pagination">
            <li class="page-item disabled"><a href="#"><i class="fa fa-angle-double-left"></i></a></li>
            <li class="page-item"><a href="#" class="page-link">1</a></li>
            <li class="page-item"><a href="#" class="page-link">2</a></li>
            <li class="page-item active"><a href="#" class="page-link">3</a></li>
            <li class="page-item"><a href="#" class="page-link">4</a></li>
            <li class="page-item"><a href="#" class="page-link">5</a></li>
            <li class="page-item"><a href="#" class="page-link"><i class="fa fa-angle-double-right"></i></a></li>
        </ul>
    </div>*@
@*</div>
        </div>
    </div>*@   